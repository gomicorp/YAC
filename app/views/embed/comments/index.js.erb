// 이 파일은 페이지네이션 "더 보기" 의 결과물을 전달합니다.

commentService.displayRequestedPage({
  nextPage: parseInt('<%= @pagy.next.to_i %>') || false,
  commentsMarkUp: '<%= j(comment_component.comments @comments) %>',
});
